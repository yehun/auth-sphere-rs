[package]
name = "lib-mfa"
version = "0.1.0"
edition.workspace = true
authors.workspace = true

[dependencies]
thiserror.workspace = true
rand = "0.8"
base32 = "0.4"
totp-rs = { version = "5.7.0", features = ["gen_secret", "otpauth", "qr"] }

